<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.myexample.abhi.boollook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/bkicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/bkicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Boollook">
        <activity android:name=".BookActivity"></activity>
        <activity android:name=".BookSearch">

        </activity>
        <activity android:name=".CsBooks" />
        <activity android:name=".CivilBooks" />
        <activity android:name=".MechBooks" />
        <activity android:name=".AllBooks" />
        <activity android:name=".DashBoard" />
        <activity android:name=".Signup" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>